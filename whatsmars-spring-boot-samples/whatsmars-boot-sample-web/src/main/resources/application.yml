spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
#  autoconfigure:
#    exclude:
#      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

server:
  servlet:
    context-path: /mars
  port: 8080
  tomcat:
    uri-encoding: UTF-8
    basedir: /tmp/logs/whatsmars-spring-boot
    accesslog:
      directory: logs
      enabled: true
      pattern: common
      prefix: access_log
      suffix: .log